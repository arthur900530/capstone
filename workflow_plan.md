1. create a workflow class that stores workflows in predefined structures (such as trees)
2. create a custom tool that allows the skill ingestor model to extract the workflow, alongside the timestamps within the provided video, from the given media while at the same time build skill.md
3. during the skill ingestion process add a page where the video is demonstrated alongside the corresponding workflow steps, the workflow step should be mapped clearly according to the timestamp within the video
4. on the evaluation page change the trajectory display accordingly

